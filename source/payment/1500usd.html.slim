---
title: XSCALE Coach Training Installment (USD)
---
/! Main
#main.wrapper.style1
  .container
    header.major
      h2 Payments
    /! Content
    section#content
      .content
        header
          <!-- Load Stripe.js on your website. -->
          <script src="https://js.stripe.com/v3"></script>

          <!-- Create a button that your customers click to complete their purchase. -->
          h3 XSCALE Coach Training Installment
          a.button id="checkout-button" Pay $1,500.00 USD
          div id="error-message"

          <script> var stripe = Stripe('pk_live_0vHGBpMIGCdZ9P2cirSNcKkU', { betas: ['checkout_beta_4'] }); var checkoutButton = document.getElementById('checkout-button'); checkoutButton.addEventListener('click', function () { stripe.redirectToCheckout({ items: [{sku: 'sku_EMZ589JPLJHDPS', quantity: 1}], successUrl: 'http://xscalealliance.org/paid.html', cancelUrl: 'http://xscalealliance.org/1100AUD.html', }) .then(function (result) { if (result.error) { var displayError = document.getElementById('error-message'); displayError.textContent = result.error.message; } }); }); </script>